services:
  - name: Printer
    setup: icecap.servers.printer.setup
    replicated: both
    nodes:
      - node1
      - node2

# For illustration only:
# The Logger service is not replicated and runs on a restricted set of nodes.
# The database service has both non-replicated and replicated adapters.

unused-services:
  - name: Logger
    run: servers/logger
    nodes:
      - node1
      - node4

  - name: Database
    run: servers/db
    args: debug
    replicated: both
